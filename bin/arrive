#!/bin/bash

CONFIG_PATH="$(cd;pwd)/.config"

LINKED_FILES=(
  pylintrc
  zshrc
  gitconfig
)

PROGRAMS=(
  zsh
  vim
  neovim
  hlint
  ghcmod
  git
  gcc
  g++
  direnv
  python3
  python3-pip
  nodejs
  npm
  mosh
  wget
  swi-prolog
)

PYTHON_PACKS=(
  setuptools
  virtualenv
  flask
  numpy
  pandas
  scikit-learn
  tensorflow
  #tensorflow-gpu
  #jupyter
  https://github.com/joh/when-changed/archive/master.zip
)

HASKELL_PACKS=(
  base
  hoogle
  parallel
  hspec
)

NPM_PACKS=(
  marked
  meteor
  mocha
  react
  underscore
)


# start at home
echo "Linking Dot-files"
for FILE in "${LINKED_FILES[@]}"
do
    mv ".$FILE" "old_$FILE"
    ln $(echo "$CONFIG_PATH/$FILE") $(echo "$(cd; pwd)/.$FILE")
done

if hash brew 2>/dev/null; then
  echo "Installing Software"
  brew install --appdir="/Applications" ${PROGRAMS[*]}
  echo "Installing mac specific programs"
  brew cask install homebrew/cask/alfred
else
  echo "Updating Apt"
  sudo apt-get update
  echo "Installing Software"
  sudo apt-get -f install ${PROGRAMS[*]}
fi

# install oh-my-zsh
sh -c "$(wget https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh -O -)"

ln ~/.config/bullet-train.zsh-theme ~/.oh-my-zsh/custom/bullet-train.zsh-theme

# Install plug
curl -fLo ~/.local/share/nvim/site/autoload/plug.vim --create-dirs \
    https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim

nvim +PlugInstall

echo "Installing Python Libraries"
pip install --upgrade pip
pip install ${PYTHON_PACKS[@]}

#Install stack
curl -sSL https://get.haskellstack.org/ | sh

echo "Installing Haskell Packages"
stack install ${HASKELL_PACKS[@]}

echo "Install NPM Packages"
npm update
npm install ${NPM_PACKS[@]} -g

echo "Starting zsh"
zsh
